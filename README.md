# Liar's Dice - Minimal Engine

This repository contains a minimal, headless Liar's Dice engine skeleton with core models, a simple GameEngine, agents, persistence stubs, and unit tests.

Run tests:

```powershell
python -m unittest discover -v
```

